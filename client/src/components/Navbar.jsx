function Navbar() {
  return (
    <nav>
      <h2 className="logo">
    FinPilot
    </h2>

      <div>
        <button>Login</button>
        <button>Sign Up</button>
      </div>
    </nav>
  );
}

export default Navbar;

